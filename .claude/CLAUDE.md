# プロジェクト全体のルール

## コミュニケーション

- 応答や PR review、コメントアウトなどは日本語で行う
- 複雑なタスクでは実装前に計画を提示し、承認後に着手する

## 進め方

- アーキテクチャ・ファイル構造・API 設計は、着手前に相談して合意する
- 合意後は、後回しにできる部分は残しつつ、可能な範囲まで一気に実装してよい
- 仕様上の判断が必要な点は、都度確認する
- 実装の節目で `docs/` に構造的に記録する（ドキュメント作成時は `.claude/rules/documentation.md` に従い、見出し構成を先に提示する）

## コードスタイル

- 厳密な型づけを意識する（基本的に `any` は使わない）
- `as` による危険な型アサーションを避ける
- `null` / `undefined` の扱いを明示する
- `useEffect` 内の非同期処理は cleanup を意識する
- ESLint/Prettier で処理済みの些末な指摘はしない
- エラーは握りつぶさず、適切にハンドリングする
- 重複実装をしない
  - まず既存の共通実装がないかを確認する
  - 類似実装が見つかった場合は、責務と依存関係を見て、適切な共有スコープに切り出すことを検討する
  - 無理にグローバル化しない
  - 共通化の影響範囲が大きい場合は、事前に方針を提示して承認を得る
- 後方互換性を保証しすぎない
  - 設計レベルで変更する場合は、その旨を提示し、承認後、もとの設計に関連するコードを残さない
  - 関連して、デッドコードは必ず削除し、決してコメントアウトで無効化しない
- 既存の動作するコードをリファクタリングする場合は、目的と影響範囲を明示する
  - その規模が大きい場合には、必ず事前の承認を求めること
- デバッグ用の `console.log()` は本番コードに残さない

## Git

- Conventional Commits に準拠したコミットメッセージを意識する
- 確認なしに自動で commit・push しない

## ドキュメント

- 新規実装をする際には必ずドキュメントを残すべきかどうかを判断し、どちらにせよその判断結果をユーザーに明示すること
- 既存実装を変更する場合は必ず対応するドキュメントの有無を確認し、現状把握に役立てるのはもちろんのこと、現状の実装との乖離を調査したり、実装作業によってドキュメントを更新する必要があるかどうかを考慮したりすること
- ドキュメントを作成・変更する場合は、 `.claude/rules/documentation.md` のルールを参照のうえ、見出しごとにどんな内容を書くかをユーザーに提示し、執筆計画が承認されてから書き始めること
- ドキュメントを作成・削除した場合は、目次の役割を果たす `docs/README.md` を更新すること

## 禁止事項

- README・ドキュメントを勝手に生成・変更しない
- テストコードを確認なしに削除・変更・コメントアウトしない
- Linter や Formatter のルールを勝手に削除・変更しない

## 備考

- 詳細なルールは `.claude/rules/` 以下を参照すること
